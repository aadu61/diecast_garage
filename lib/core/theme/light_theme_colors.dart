import 'package:flutter/material.dart';

/*
Opacity reference

100% — FF
95% — F2
90% — E6
85% — D9
80% — CC
75% — BF
70% — B3
65% — A6
60% — 99
55% — 8C
50% — 80
45% — 73
40% — 66
35% — 59
30% — 4D
25% — 40
20% — 33
15% — 26
10% — 1A
5% — 0D
0% — 00

*/

class LightColor {

  // Primary Accent
  static const accPrimary = Color(0xFFC73020);
  static const accHover = Color(0xFFE8501F);

  // Backgrounds
  static const bgBase = Color(0xFFFFFFFF);
  static const bgSurface = Color(0xFFF5F3EF);
  static const bgElevated = Color(0xFFEEEBE5);
  static const bgOverlay = Color(0xFFE2DDD5);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF666666);
  static const textHint = Color(0xFFAAAAAA);

  // Special
  static const gold = Color(0xFF8A6A1A);

  // Badges
  static const badgeSuper = Color(0xFF7C3AED);
  static const badgeRare = Color(0xFFD97706);
  static const badgeStd = Color(0xFF9CA3AF);

  // Border
  static const border = Color(0xFFE2DDD5);
}
