import 'package:flutter/material.dart';

class DarkColor {

  // Primary Accent
  static const accPrimary = Color(0xFFE84030);
  static const accHover = Color(0xFFFF6B35);

  // Backgrounds
  static const bgBase = Color(0xFF0F0F0F);
  static const bgSurface = Color(0xFF1A1A1A);
  static const bgElevated = Color(0xFF242424);
  static const bgOverlay = Color(0xFF2E2E2E);

  // Text
  static const textPrimary = Color(0xFFF5F3EF);
  static const textSecondary = Color(0xFFAAAAAA);
  static const textHint = Color(0xFF666666);

  // Special
  static const gold = Color(0xFFC9A84C);

  // Badges
  static const badgeSuper = Color(0xFF7C3AED);
  static const badgeRare = Color(0xFFB45309);
  static const badgeStd = Color(0xFF374151);

  // Border
  static const border = Color(0xFF2E2E2E);
}
